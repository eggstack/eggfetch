//! Client configuration.

/// Client configuration placeholder.
///
/// Concrete fields (pool sizing, TLS config, redirect policy, etc.) are
/// filled in by later milestones.
#[derive(Debug, Default, Clone)]
pub struct Config {
    _private: (),
}
