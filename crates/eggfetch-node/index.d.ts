// Experimental prototype stub — NOT generated declarations.
//
// `eggfetch-node` is an explicitly experimental prototype (see
// `docs/architecture/ffi-and-node.md`). There are no generated TypeScript
// declarations in this milestone: this file is intentionally declaration-free
// so no consumer can mistake it for a supported typed surface. A supported
// binding must replace this stub with declarations generated from the Rust
// exports (e.g. via the napi-rs CLI) plus a TypeScript compile-smoke fixture.

export {};
