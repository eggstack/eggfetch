# Release and Verification M001A — 0.2.1 Release-Candidate Preparation

Status: ready

Repository baseline: `36ab862dabf8f30fdcf05900a68fdf9f93060879`

Parent milestone:

- M001 coordinated 0.2.x publication

Depends on:

- M006C2 closed
- live Stage C binding
  `5247ff0e01e309d9b408b8b4b4c90151ee5ce9fa`

Unblocks:

- M002 Python 3.15 wheel rehearsal

Primary class: infrastructure / release preparation

## 1. Objective

Create one immutable, internally consistent `0.2.1` release-candidate commit
from the current qualified tree.

This pass owns version identity, changelog/release metadata, candidate
qualification, and the exact SHA that M002 and M001B must consume. It does not
publish anything.

## 2. Current release identity

At the planning baseline:

- all six publishable crates are `0.2.0`;
- `crates/eggfetch-python/pyproject.toml` is `0.2.0`;
- `CHANGELOG.md` has a `0.2.0` section;
- GitHub `v0.2.0` already exists and targets
  `8959ca890ee34f4cf456aed648315322f1e83ef7`;
- a GitHub Release for `v0.2.0` already exists;
- `v0.2.1` does not exist at this baseline.

Do not move or reuse `v0.2.0`.

## 3. Required release-candidate changes

Bump coordinated version identity to exactly `0.2.1` in:

- `crates/eggfetch-http-connect/Cargo.toml`;
- `crates/eggfetch-core/Cargo.toml`;
- `crates/eggfetch-cli/Cargo.toml`;
- `crates/eggfetch-ffi/Cargo.toml`;
- `crates/eggfetch-python/Cargo.toml`;
- `crates/eggfetch-node/Cargo.toml`;
- `crates/eggfetch-python/pyproject.toml`;
- internal publishable dependency requirements;
- `Cargo.lock` self-package versions as generated by Cargo.

Update `CHANGELOG.md` with a `0.2.1` section covering only changes since
the historical `v0.2.0` tag.

The changelog must distinguish:

- qualification/test/benchmark hardening;
- planning/documentation changes;
- any actual runtime/user-visible changes, if present.

Do not copy stale `0.1.9` release text forward.

## 4. Candidate-delta audit

Audit:

`8959ca890ee34f4cf456aed648315322f1e83ef7..candidate`

and separately:

`5247ff0e01e309d9b408b8b4b4c90151ee5ce9fa..candidate`

The first audit explains what `0.2.1` contains beyond the already-created
`v0.2.0` release identity.

The second audit decides whether Stage C remains valid.

Expected post-Stage-C delta is release metadata plus docs/planning/CI-surface
cleanup only. If runtime source, test semantics, validation scripts,
dependency versions/sources, feature defaults, compatibility profiles, or
packaging workflow semantics changed after the live freeze, stop and apply
ADR-0003 requalification before selecting the candidate.

## 5. Candidate qualification

Run from a clean worktree:

```bash
./scripts/check.sh
./scripts/check.sh extended
./scripts/check.sh package
git diff --check
```

Run the time-sensitive release security preflight only immediately before
actual crates.io publication in M001B. M001A may run it diagnostically, but
that does not satisfy M001B's live preflight.

Verify:

- all six crate versions = `0.2.1`;
- Python distribution version = `0.2.1`;
- internal dependency requirements use `0.2.1`;
- `python scripts/validate_release_versions.py` passes;
- `python scripts/validate_publishable_internal_dependencies.py` passes;
- no `v0.2.1` tag exists;
- candidate worktree is clean.

## 6. Candidate freeze

After all gates are green, record the exact commit SHA as
`0.2.1 release candidate`.

No further repository change may be folded into that candidate.

M002 must execute against this exact commit. M001B must later tag and publish
this exact commit.

Docs-only closure records may be descendants, but they do not replace the
candidate SHA.

## 7. Compatibility / Stage C disposition

A coordinated version bump alone is release identity, not a runtime semantic
change.

If the candidate audit proves no executable or qualification-input change
relative to the live freeze, Stage C remains
`5247ff0e01e309d9b408b8b4b4c90151ee5ce9fa`.

If that proof fails, do not claim the old Stage C binding covers the
candidate. Open/requalify first.

## 8. Explicit non-goals

- No crates.io publication.
- No tag creation.
- No PyPI dispatch.
- No runtime/API refactor.
- No dependency upgrades.
- No workflow redesign.
- No HTTPX 1.0/H3/Node maturation work.
- No release-version fallback to `0.2.0`.

## 9. Acceptance criteria

- [ ] all coordinated manifests/pyproject are exactly `0.2.1`;
- [ ] internal dependency versions are coherent;
- [ ] changelog contains an accurate `0.2.1` section;
- [ ] `v0.2.0` remains untouched;
- [ ] `v0.2.1` is absent before publication;
- [ ] tag-to-current delta and Stage-C-to-candidate delta are classified;
- [ ] Tier 1 green;
- [ ] Tier 2 green;
- [ ] Tier 3 green;
- [ ] release/version topology validators green;
- [ ] candidate worktree clean;
- [ ] exact candidate SHA recorded;
- [ ] Stage C disposition explicitly recorded.

## 10. Closure

Create:

`plans/closure/release-verification/001a-0-2-1-release-candidate-preparation.md`

Record candidate SHA, version audit, both delta audits, Tier 1/2/3 results,
Stage C disposition, and the explicit handoff to M002.

After closure, M002 becomes the sole ready release task.
