# Resolved differences ledger — HTTPX2 2.12.0 (Stage C qualified)
# Historical records of previously-allowed differences that have been
# resolved (implemented and verified). No entries: intentional differences
# remain in `allowed-differences.toml`; entries are appended only after
# implementation + differential evidence.
